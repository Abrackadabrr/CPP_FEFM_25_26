#include <iostream>

int main() {
    int n;
    std::cin >> n;
    int array[n];
    // а дальше считываем из файла
    array[0] = 2;
    // компилятор это пропустит, но как это трактовать неясно

    // можно сделать большой массив
}
